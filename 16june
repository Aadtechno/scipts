I need to create a new minimal Angular SPA application to test header-based canary deployment on GKE with Istio.

Please generate the complete working source code and deployment-related files.

Requirement:

1. Application Type

* Create a minimal Angular single-page application.
* UI should be simple and clean.
* The application source code should NOT contain any logic related to canary deployment, stable deployment, Istio routing, or Kubernetes deployment strategy.
* Canary/stable behavior should be controlled only through image version, Helm values, Kubernetes labels, and Istio routing.

2. UI Requirement

* Single page UI is enough.
* Display a simple title such as:
  `Angular Canary Deployment Test App`
* At the bottom of the page, display a short build/version text.
* Example:
  `Version: 1.0.0`
  or
  `Build: stable-1.0.0`
  or
  `Build: canary-1.0.1`
* This version should be configurable during build or image creation, not hardcoded in multiple places.

3. Version Handling

* When the Docker image version/tag is updated, the app should show that version at the bottom of the UI.
* Please implement a clean approach for injecting the version into the Angular app.
* Explain how the version value is passed during build.
* Avoid manually changing source code every time the image version changes.

4. Angular Base Path / Prefix Requirement

* The app will be accessed behind an Istio URL like:
  `https://istio.dev.gcp-intl.gcp.kxv/avro-record`
* Please explain whether Angular needs a base href or deploy URL prefix.
* Provide a way to configure the path prefix using Helm values.
* Example prefix:
  `/avro-record/`
* The same image should ideally work with different path prefixes if possible.
* If runtime configuration is needed for base path, explain the best approach.
* If build-time base href is required, explain clearly why.

5. Docker / Image Build

* Provide a Dockerfile for the Angular app.
* Use a multi-stage build:

  * Node stage for Angular build
  * Nginx or company-approved web server image for serving static files
* Leave placeholders for company GAR base images.
* Example:
  `FROM <COMPANY_GAR_NODE_IMAGE> AS build`
  `FROM <COMPANY_GAR_NGINX_IMAGE> AS runtime`
* Mention which Node.js version you are choosing and why.
* Explain compatibility between Angular version and Node.js version.
* Do not use public Docker Hub images directly in the final answer except as comments/examples.

6. GitHub Workflow

* Create a GitHub Actions workflow to build and bake/push the Docker image.
* Leave the runner blank or as a placeholder because I will update it.
* Example:
  `runs-on: <UPDATE_RUNNER_HERE>`
* Include placeholders for:

  * GAR registry
  * project ID
  * image name
  * image tag
  * authentication step
* The workflow should support passing image tag/version to the Docker build.
* The same version should be displayed in the Angular UI.

7. Helm Chart

* Provide a minimal Helm chart for deploying the Angular SPA.
* Include:

  * Deployment
  * Service
  * ConfigMap if needed
  * values.yaml
* Helm values should allow configuration of:

  * image repository
  * image tag
  * replica count
  * app path prefix such as `/avro-record/`
  * environment name
  * app version/build version
  * labels for stable/canary separation
* Do not include canary-specific logic inside Angular source code.
* Canary/stable separation should be done using Helm release name, labels, image tag, and Istio VirtualService.

8. Istio Requirement

* Provide sample Istio VirtualService and DestinationRule examples for header-based routing.
* Routing behavior:

  * No header → stable deployment
  * Header `x-qa-group: beta` → canary deployment
* App URL should remain the same:
  `https://istio.dev.gcp-intl.gcp.kxv/avro-record`
* QA should test canary using the same URL with request header:
  `x-qa-group: beta`
* No separate DNS should be required.

9. Testing Instructions

* Provide commands to test:

  * Stable without header
  * Canary with header
  * Repeated curl test to confirm no accidental canary routing without header
  * Browser testing using ModHeader
  * Bruno testing with request header
* Include commands to verify pods, labels, service endpoints, Istio routes, and logs.

10. Output Format
    Please provide:

* Full project folder structure
* Full source code for all important files
* Dockerfile
* GitHub Actions workflow
* Helm chart files
* Istio YAML examples
* Build and deployment commands
* Testing commands
* Explanation of Node version choice
* Explanation of Angular base href/path prefix handling
* Notes on what should remain outside application source code

Important:
Act as a Senior DevOps Engineer. Keep the solution production-like but minimal enough for NPE testing.
